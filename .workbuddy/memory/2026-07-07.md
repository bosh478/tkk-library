# 2026-07-07 工作记录

## tkk-source-fetch 在 WorkBuddy 沙箱跑通（跨境礼品卡项目）
- 用户需求：`/tkk-source-fetch` 按 `D:\BaiduSyncdisk\第二大脑\1-Projects\跨境礼品卡` 目录内 URL 下载资料。
- 既有扫描清单可用：`.scan/2026-07-07_scan_跨境礼品卡.url-list.txt`（92 条唯一 URL，已验证）。无需重扫（重扫因 python 沙箱问题出 0 条）。
- 抓取 run-id：`2026-07-07_跨境礼品卡_资料抓取`
- 产出位置：`X:\home\kangkang\tkk-library\Clippings\inbox\2026-07-07_跨境礼品卡_资料抓取\`（每 URL 三件套：`NNN-slug.md` + `.meta.json` + `_raw/NNN-raw.{html|pdf}`），manifest 在 `_manifest.json`。

## 关键环境坑 & 修复（可复用）
- **根因**：WorkBuddy 沙箱里托管的 `python3`（C:\ProgramData\WorkBuddy\users\1812650b\.workbuddy\binaries\python\versions\3.13.12\python3）被关在沙箱里，**只能看见 /tmp，完全看不到 /x(X盘)、/d(D盘)、/c**。导致 skill 内所有 `python3` 对挂载盘文件的读写全失败（扫描出 0 条、inbox 写不出 manifest）。
- **修复**：在 `/c/ProgramData/WorkBuddy/users/1812650b/bin/python3` 放一只 POSIX→Windows 路径包装脚本，把 argv 与关键环境变量里的 `/x /d /c /tmp /home` 绝对路径用 `cygpath -w` 翻成 Windows 盘符路径，再 exec 系统原生 python `D:\AI agent\python\python.exe`（3.14.3，能看 X/D/C 盘）。调用时 `export PATH="/c/ProgramData/WorkBuddy/users/1812650b/bin:$PATH"` 让包装脚本优先。
- 另：`flock` 在 mingw 缺失，已在同 bin 目录放了一个恒成功的 `flock` shim（单线程用，安全）。
- tkk-source-fetch 脚本原硬编码 `/home/kangkang/...`，已改为读 `TKK_LIBRARY` / `TKK_SKILL_ROOT` 环境变量（默认仍是原路径）。
- 注意：包装脚本只翻译「独立 argv 路径参数」和「已知名环境变量」，写在 `python3 -c "..."` 代码串内部的路径字符串不会被翻译（需把路径作为单独 argv 传入）。

## 验证结果
- PDF 路径：pdftotext 提取，`denoise_status=pdftotext_ok` ✅
- HTML 路径：tkk-denoise 降噪，标题/正文正确提取，`denoise_status=ok` ✅
- 全量 92 条已在后台启动（task_id 8vnLA6），完成后需核对 .md 数量与 `_fetch_errors.log`。
