# CLAUDE.md — LLM Wiki Schema

本文件是 LLM Wiki 的 Schema，定义 wiki 的结构、约定和工作流程。LLM 应遵循本文件的规范维护 wiki。

---

## 核心理念

wiki 是一个**持久化、累积性的知识库**。它不是每次查询时从原始文档重新检索的 RAG 系统，而是 LLM 增量构建和维护的结构化知识体。

交叉引用已在其中。矛盾已标注。综合已形成。新资料进入时，LLM 负责将其整合进已有 wiki，而非仅做索引。

---

## 三层架构

### 1. Raw Sources（原始资料）

路径：`/sources/`

- 存放来源文件，**不可直接修改**
- 分类组织：`/sources/裁判文书/`、`/sources/法律法规/`、`/sources/网络文章/`、`/sources/书籍资料/`
- 每个来源文件附带元数据注释（来源 URL、获取日期、文件类型）

### 2. Wiki（维基页面）

路径：`/wiki/`

LLM 完全拥有的知识层，所有 wiki 页面存于此：

| 子目录 | 内容 |
|--------|------|
| `entities/` | 实体页：案件、法条、机构、人物 |
| `concepts/` | 概念页：罪名、制度、法律原则、程序 |
| `syntheses/` | 综合分析页：专题研究、对比分析、案件复盘 |
| `summaries/` | 来源摘要页：每个原始资料的消化摘要 |

### 3. Schema（本文件）

即本文件，定义工作规范。

---

## 文件命名约定

- **Wiki 页面**：`类型_标题.md`（如 `concept_受贿罪.md`、`entity_最高法2026解释二.md`）
- **来源文件**：保留原始文件名，仅在顶部加 YAML frontmatter 记录元数据
- **目录**：全小写，用下划线分隔（如 `source_documents`）

---

## Wiki 页面格式

每个 wiki 页面必须包含：

```markdown
---
title: 页面标题
type: concept | entity | synthesis | summary
created: YYYY-MM-DD
updated: YYYY-MM-DD
tags: [标签1, 标签2]
sources: [关联来源文件列表]
---

## 正文
```

- 正文用 Markdown 格式
- 法条引用格式：`《刑法》第XX条`（无书名号内空格）
- 案例引用格式：`[案件名]（[案号]）`
- 重要论断需标注来源：`① 来源：xxx`

### Summary 页面附加要求

`wiki/summaries/` 下的摘要页 **必须** 在 frontmatter 中包含 `category` 字段，用于自动汇总到 index.md：

```yaml
---
title: 律师办理公司对外担保业务操作指引2024
type: summary
published: 2024-12-17
created: 2026-04-14
tags: [律师业务指引]
category: 诉讼与仲裁
source: [[20241217_律师办理公司对外担保业务操作指引2024]]
---
```

**NotebookLM 风格格式（含以下全部或部分 section）**：

```markdown
## 📋 文档概览

| 字段 | 内容 |
|------|------|
| 来源 | [[源文件名]] |
| 类型 | 律师业务指引 / 司法解释 / 案例分析 / ... |
| 发布日期 | YYYY-MM-DD |
| 关键词 | tag1 / tag2 / tag3 |
| 核心要点 | 3-5 条精炼摘要 |

## ❓ 常见问题

> 问：...
> 答：...

## 📅 时间线（立法类文档适用）

| 时间 | 事件 | 主要变化 |
|------|------|----------|
| YYYY-MM | 版本/解释X发布 | 主要内容... |

## 正文
```

**格式说明**：
- `## 📋 文档概览`：NotebookLM 的 Source Overview Card，5 列结构化元数据
- `## ❓ 常见问题`：AI 自动生成的 FAQ，增强页面交互性
- `## 📅 时间线`：立法进程、解释演进类文档适用，Timeline 格式

> 注意：
> - 书籍摘要（刑法释义、司法解释全书、审判指导案例、公诉人指南）不归入 summaries，而是移入 `wiki/concepts/` 或 `wiki/syntheses/`，不使用 category 字段。
> - FAQ 和时间线为建议添加项，文档概览为必填项

category 取值：
- **律师业务指引类**（存放于 `wiki/summaries/`）：
  - 诉讼与仲裁 / 公司商事与合规 / 劳动与社会保障 / 医药健康 / 国际贸易 / 婚姻家庭与继承 / 建筑工程与房地产 / 政府与公共法律服务 / 生态环境与能源 / 知识产权 / 诉讼与仲裁（民商事） / 金融与财税
- **刑事参考类**（存放于 `wiki/summaries/`，仅限律师业务指引中的刑事相关内容）

---

## 交叉引用规范

- Wiki 内部链接使用 Obsidian 双向链接：`[[页面标题]]`
- 同一罪名/概念在不同条文中的差异，使用对比表格呈现
- 相关概念页之间，在底部 `## 相关` 区域互相链接
- 矛盾或更新关系，在页面中明确标注：`⚠️ 与 [[xxx]] 存在冲突，以[[xxx]]为准`

---

## 工作流程

### 批量 Ingest 阶段门（三阶段）

处理 5 个以上文件时，必须遵循：

1. **阶段一（Agent 执行）**：并行 Agent 或 batch script 处理文件。**禁止在此阶段更新 index.md 或 log.md。**
2. **阶段二（清理验证）**：检测截断文件（< 5KB 或行数 < 20 → 可疑）；去重（同名保留最大）；确认 sources/summaries 一一对应。
3. **阶段三（索引更新）**：从 summary 的 `category` 字段汇总分类，生成 index.md 条目，写入 log.md。

> ⚠️ 阶段门是硬性规则。阶段一完成前不得执行阶段二/三，并行 Agent 运行期间主会话不做任何不可逆写操作。

### 批量 Ingest 执行规范（经验固化 v2）

处理 10 个以上文件时，必须遵循以下规范：

#### 1. 处理前：源文件清单分析（必做）

**必须先分析再处理，禁止盲目启动 Agent**：
- 执行 `ls sources/xxx/*.md | wc -l` 获取源文件总数
- 检查源文件命名是否统一（可能存在"指导案例"vs"指导性案例"等变体）
- 识别特殊命名模式（如文件名与编号之间无空格、编号异常等）
- 预判目标目录是否有已有文件（避免覆盖或遗漏）
- **检查源文件 frontmatter 的 `structure` 字段长度**：如果结构目录只有 4-6 行，说明源文件内容有限，Agent 的输出规模也会偏小——这**不是 Agent 失败**，而是预期行为

#### 2. 并行 Agent 策略（关键改进）

**Agent 报告不可信，必须用脚本验证**：

| 问题 | 根因 | 解决方案 |
|------|------|----------|
| Agent 报告"处理 3-5 个" | 上下文限制 | **立即用脚本验证**：`ls wiki/*.md \| wc -l` |
| Agent 报告"全部完成" | 未检查实际文件 | **精确比对** source vs target |
| 数量对不上 | 覆盖/遗漏 | **主会话接管处理遗漏** |

**并行 Agent 执行规范**：
- 启动多批 Agent 并行处理（每批 2-4 个）
- **每批完成后立即验证数量**，不等待所有批次完成
- 验证脚本：
```bash
# 精确比对：找出未处理的 source 文件
for f in sources/xxx/*.md; do
  name=$(basename "$f" .md)
  base_clean=$(echo "$name" | sed 's/（[0-9]*年[修正修订]*）//g')
  [ -f "wiki/concept_${name}.md" ] || [ -f "wiki/concept_${base_clean}.md" ] || echo "$name"
done | wc -l
```

#### 3. 命名差异处理（必须覆盖）

Source 文件名与 concept 文件名可能存在差异：

| 差异类型 | Source | Concept |
|----------|--------|---------|
| 年份后缀 | `中华人民共和国XX法（2020修正）.md` | `concept_中华人民共和国XX法.md` |
| 修订 vs 修正 | `XX法（2023修订）.md` | `concept_XX法.md` |
| 无后缀 | `中华人民共和国XX法.md` | `concept_中华人民共和国XX法.md` |

**模糊匹配脚本**（处理命名差异）：
```bash
base_clean=$(echo "$name" | sed 's/（[0-9]*年[修正修订]*）//g')
```

#### 4. 数量核验：阶段门硬性检查

**Phase 1 完成后、Phase 2 开始前，必须执行**：

```bash
# 阶段门检查：数量必须完全匹配
SOURCE_COUNT=$(ls sources/xxx/*.md | wc -l)
TARGET_COUNT=$(ls wiki/summaries/*.md | wc -l)
echo "Source: $SOURCE_COUNT, Target: $TARGET_COUNT"
[ "$SOURCE_COUNT" -eq "$TARGET_COUNT" ] || echo "⚠️ 数量不一致！"
```

**数量核验失败 → 主会话接管 → 直接处理遗漏文件**

#### 5. 进度监控：阶段性验证

| 阶段 | 验证时机 | 验证方法 |
|------|----------|----------|
| Phase 1 进行中 | 每批 Agent 完成后 | 立即执行 `ls wiki/*.md \| wc -l` |
| Phase 1 全部完成 | 开始 Phase 2 前 | **脚本精确比对** |
| Phase 2 验证中 | 每项检查后 | 记录发现的问题 |
| Phase 3 更新后 | index.md 更新后 | 验证统计数字 |

#### 6. 内容质量标准

- **禁止生成占位符内容**："（待补充具体内容）" 等空内容违背 wiki 持久化知识库理念
- Phase 1 输出的摘要必须是从源码提取的实际内容（关键段落、要点列表）
- 不合格内容必须在 Phase 2 返工，不得携带缺陷进入 Phase 3

#### 7. frontmatter 字段一次性完备

写入文件前，**必须一次性确认所有必需字段**：
- `type: summary` + `category` 必填（用于 index.md 自动汇总）
- `tags` 包含业务类型 + 程序类型
- `source` 指向原始来源文件
- `created` / `updated` 时间戳

字段遗漏会导致返工（重新运行脚本追加字段）。

#### 8. 批量规模适配索引策略

| 规模 | 索引策略 |
|------|----------|
| < 20 条 | 逐条列出（现有律师业务指引格式） |
| ≥ 20 条 | **类目概述 + 目录 glob 引用**，避免 index.md 臃肿 |

示例（≥20条时）：
```markdown
### 最高检指导性案例（61条）
> 详见 [[wiki/summaries/]] 目录下 SPP_GuideCase_*.md
```

#### 9. Phase 2 验证是硬性门控

阶段二**必须执行全部检查项，不得跳过任何一项**：

- **数量核验**：`源文件数 == 目标文件数`（最关键！）
- **断链检查**：`grep -r "\[\[" wiki/` 查找未解析链接
- **尺寸验证**：文件 < 5KB 或行数 < 20 → **不一定截断**，可能是"只提取摘要"的正常输出（如案例摘要只保留核心要点）。判断方法：
  - 先抽检内容：读取文件查看正文是否有实质内容（非占位符）
  - 再对比源文件：如果源文件就是目录型/摘要型结构，输出规模偏小是正常的
  - 仅当文件内容空洞或与源文件规模严重不符时才判定为截断
- **内容抽检**：每个类别随机抽 3 个文件，确认：
  - frontmatter 关键字段存在：`type`、`tags`、`source`、`created`/`updated`
  - 正文前 100 字符非空（不是占位符、不是标题重复）
  - 内容与源码主题匹配
- **命名验证**：脚本命名逻辑与实际文件名对照（抽检 3-5 个）

> ⚠️ **Agent 报告成功不等于内容合格**。内容抽检是防止"Agent 报告成功但内容是空壳或截断"的最后防线，**任何规模都必须执行**。

Phase 2 不合格 → 返回 Phase 1 返工，不得跳过。

#### 10. 批量处理前验证脚本命名逻辑

运行 batch script 前，**必须抽检 3-5 个实际文件名**，确认：
- 文件名中的日期格式（前缀、中缀）与脚本的 `extract_date()` 正则匹配
- `f.stem` 用法是否引入不必要的路径层级

---

### Ingest（消化新资料）

1. 将原始文件放入对应 `sources/` 子目录
2. LLM 阅读原始文件
3. 讨论关键要点（与用户）
4. 写入摘要页到 `wiki/summaries/`
5. 更新 `index.md` 中的相关条目
6. 在 `log.md` 中记录 ingest 事件
7. 检查是否需要更新已有的 entity/concept 页面

### Query（回答问题）

1. 读取 `index.md` 定位相关页面
2. 读取相关 wiki 页面
3. 综合回答，标注来源
4. **重要**：如果回答产生有价值的新内容（分析、对比、发现），将其写入 wiki 作为新页面

### Lint（健康检查）

定期（每次重要 ingest 后或每周）执行：

1. 检查断链（orphan pages）
2. 检查矛盾（同一问题在不同页面的说法是否一致）
3. 检查过时（较新的来源是否推翻旧结论）
4. 检查空白（重要概念是否已有页面）
5. **验证 index.md Summaries 条目数与 wiki/summaries/ 文件数一致，各分类计数与实际条目匹配**
6. 将检查结果记入 `log.md`

---

## index.md 格式

```markdown
# Index

## Entities（实体）
| 页面 | 摘要 | 更新日期 | 来源数 |
|------|------|----------|--------|

## Concepts（概念）
| 页面 | 摘要 | 更新日期 | 相关法条 |

## Syntheses（综合）
| 页面 | 摘要 | 日期 |

## Summaries（来源摘要）
| 来源 | 摘要 | 日期 |
```

---

## log.md 格式

```markdown
# Log

## [YYYY-MM-DD] ingest | 来源标题
- 操作：消化 xxx
- 创建/更新页面：[[页面1]] [[页面2]]
- 备注：...

## [YYYY-MM-DD] query | 用户问题摘要
- 涉及页面：[[页面x]]
- 关键结论：...

## [YYYY-MM-DD] lint
- 检查结果：...
- 发现问题：...
```

---

## 输出格式优先级

面对问题或分析任务时，默认输出优先级：

1. **对比表格**（罪名对比、制度对比、情节对比）
2. **ASCII 流程**（程序步骤、因果链条）
3. **结构化清单**（构成要件、证据清单、风险点）
4. **时间线**（立法进程、案件经过）
5. 文字仅作为图表的补充说明

---

## 域特定规范（法律）

- 罪名体系按犯罪类型分类（贪腐类、暴力类、财产类等）
- 每个罪名页面包含：构成要件、立案标准、量刑档次、相关司法解释、典型案例
- 法条修改历史在对应概念页中记录
- 司法解释与刑法条文的关系明确标注（解释重点、补充、修改）

---

## 多维索引规范（支持复杂查询）

为支持"复杂查询"和"疑难问题查询"，wiki 采用多维度交叉索引，而非单一分类。

### 五大索引维度

| 维度 | 说明 | 典型查询 |
|------|------|----------|
| **罪名维度** | 按刑法分则章节，每章一个或多个 concept 页 | "贪污罪与职务侵占罪的界限" |
| **业务维度** | 按律师业务领域，summaries 按 category 归类 | "公司对外担保合规审查要点" |
| **程序维度** | 刑事/民事/行政/仲裁程序概念 | "刑事合规整改第三方监督评估程序" |
| **法条维度** | 按重要法条索引相关页面 | "《刑法》第388条斡旋受贿的认定" |
| **解释维度** | 司法解释独立页面，关联相关罪名 | "法释〔2026〕6号解释（二）" |

### 维度交叉引用规则

1. **Concept 页的 `related` 字段**必须列出同章节相关罪名
2. **Concept 页底部 `## 相关`** 必须链接法条 entity 和同类概念
3. **Summary 页的 `tags`** 应包含业务类型 + 程序类型（如 `[公司合规, 刑事诉讼]`）
4. **Entity 页（司法解释）** 的 `tags` 必须包含解释文号和涉及罪名

### 文件类型与目标目录对照

| 文件类型 | 目标目录 | frontmatter 要求 |
|----------|----------|-----------------|
| 律师业务指引摘要 | `wiki/summaries/` | `type: summary`，`category` 必填 |
| 刑法释义章节 | `wiki/concepts/` | `type: concept`，按章节归类 |
| 司法解释全书章节 | `wiki/concepts/` | `type: concept`，关联 entity |
| 司法解释原文 | `wiki/entities/` | `type: entity` |
| 审判指导案例 | `wiki/syntheses/` | `type: synthesis` |
| 公诉人指南/程序指南 | `wiki/concepts/` | `type: concept` |

### Ingest 流程（更新版）

处理新资料时：

1. **判断文件类型**（对照上表）
2. **写入目标目录**
   - 律师业务指引 → `wiki/summaries/` + 填 `category`
   - 司法解释原文 → `wiki/entities/` + 更新相关 concept 的 `related`
   - 刑法释义/书籍章节 → 合并入 `wiki/concepts/` 对应章节
   - 审判指导案例 → `wiki/syntheses/` 或合并入对应 concept
3. **更新多维索引**：在 index.md 对应维度添加条目，在相关 concept 页添加 `related` 链接
4. **记录**：在 `log.md` 记录 ingest 事件

---

## 自我验证规则（纠错经验总结）

以下规则基于历史错误总结，生成结果前必须逐项检查：

### 1. 量刑档次完整性
**检查项**：刑法/司法解释中的量刑档次是否全部提取（三档/四档不能遗漏任一档）

**验证方法**：生成结果后，对照原文中"三年以下"、"三年以上七年以下"、"七年以上"等所有量刑幅度，确认每一档都有对应条目

**易错场景**：省略第三档（如只写两档、漏掉最高档）

### 2. 批量文件处理完整性
**检查项**：指定处理 N 个文件时，实际处理了 N 个文件

**验证方法**：
- 处理前记录文件列表 `ls *.md | wc -l`
- 处理后列出所有生成的文件，逐个核对是否遗漏
- 明确问用户："还有哪些文件未处理？"

**易错场景**：报告完成但实际漏了 2 个文件

### 3. 明确指令优先
**检查项**：是否严格按用户明确要求执行，而非自行添加步骤

**验证方法**：
- 用户说"直接安装"→ 不审计
- 用户说"不要审查"→ 不审查
- 用户说"继续创建"→ 先总结已完成的，再继续

**易错场景**：自行添加 skill-vetter 审查步骤、生成占位符内容而非实际内容

### 4. Wiki 质量标准
**检查项**：生成内容是否符合 wiki 规范

**验证方法**：
- 无导航元素、页脚、UI 残留
- 无占位符内容（`TODO`、`待补充`等）
- frontmatter 完整（type、category、source、date）
- 交叉引用有效（链接目标存在）

### 验证流程（生成后必做）

生成任何法律分析或批量处理结果后，按以下顺序自检：

1. **量刑档次检查**：对照原文件，核对所有量刑档
2. **文件数量检查**：列出应处理 vs 实际处理的数量
3. **指令依从性检查**：回顾用户要求，确认没有自行添加步骤
4. **Wiki 质量检查**：扫描输出，确认无 UI 残留、无占位符
5. **法律准确性检查**（必须）：涉及具体法条内容（数额、日期、适用范围、罪名构成等）是否引用原文；如存在不确定内容，是否已主动向用户核验

**验证脚本**（位于 vault 根目录 `scripts/`）：
- `phase2_gate.py`：Phase 2 门控检查（输出>500字符、置信度标注、无占位符、三色断言）
- `phase3_spot_check.py`：Phase 3 内容抽检（frontmatter 完整性、正文非空）

如发现问题，立即修正后再呈现给用户。

### 5. 法律条文准确性（新增硬性要求）

**检查项**：涉及法律条文的具体内容（数额标准、生效日期、适用范围、罪名构成等）是否基于原文

**验证方法**：
- 引用法条时必须注明来源文件
- 涉及多部法律/司法解释对比时，必须先读取原文再下结论
- 对数额标准、法律效力等关键数据有疑虑时，必须向用户核验

**易错场景**：
- 主观推测"以本解释为准"意味着数额标准提高（实际未修改）
- 未核实新解释条文就声称旧解释被"替代"或"失效"

**硬性规定**：不确定时主动向用户核验，**严禁编造法律条文内容**
